#include <iostream>

using namespace std;

int main()
{
    char cc;
    cin >> cc;

        if (cc=='a' || cc=='i' || cc=='u' || cc=='e' || cc=='u'|| cc=='A' || cc=='I' || cc=='U' || cc=='E' || cc=='O');
        {
            cout<<"Huruf Vokal"<<endl;
        }
return 0;
}
